Place app icon files here:

  icon.icns  — macOS
  icon.png   — Linux (512×512 recommended)
  icon.ico   — Windows

Briefcase will pick them up via the `icon = "resources/icon"` key
in pyproject.toml (it appends the platform-appropriate extension).
